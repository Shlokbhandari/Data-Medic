# DataMedic

DataMedic is a project that watches a data pipeline, automatically figures out why it broke when something goes wrong, and proposes a fix for a human to review — it does not make changes to the live pipeline on its own.
